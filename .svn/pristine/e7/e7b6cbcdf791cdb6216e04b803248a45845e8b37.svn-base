import {memberCollectionList} from '@/services/MemberCollectionList';


export default {
    namespace: 'collection',
    state: {
        loading:false,
        list: {
            data:{
             total:0,
             rows: [],
             totalPage: 5
            }
           },
      },

    effects: {
        *fetch({ payload }, { call, put } ,) {
            yield put({
              type: 'loading',
            });
            const response =  yield call(memberCollectionList,payload);
            yield put({
              type: 'save',
              payload:response
            });
          },


    },
    reducers: {
        loading(state){
          return {
            ...state,
            loading:true
          };
        },
    
        save(state, action) {
          return {
            ...state,
            list: action.payload,
            loading:false
          };
        },

    }
};