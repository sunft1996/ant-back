import React ,{ PureComponent }from 'react';
import { formatMessage, FormattedMessage } from 'umi-plugin-react/locale';
import { Row, Col, Card, Form, Input, Button, DatePicker, Alert} from 'antd';
import PageHeaderWrapper from '@/components/PageHeaderWrapper';

const Note = (
  <ul>
    <li>①请注意，按照结算日下载对账单，不是交易日。</li>
    <li>②可以下载近6个月内每日的明细对账单。</li>
    <li>③可下载外卡商户、专业化商户、直联商户的对账单明细文件。</li>
  </ul>
)
@Form.create()
class Test extends PureComponent { 
  constructor(props) {
    super(props)
  }



  render() {
    const FormItem = Form.Item;
    const { form:{getFieldDecorator} } = this.props;
    return (
      <Card>
        <Alert
          banner
          message="Informational Notes"
          description={Note}
          type="info"
          showIcon
        />
        <Form 
          style={{marginTop:"40px"}}
          layout='horizontal' 
          labelCol={{ span: 4 }} 
          wrapperCol={{ span: 14 }} 
          // onSubmit={this.handleSubmit}
        >
          <FormItem label='商户号'>
            {getFieldDecorator('num', {
                rules: [{
                  required: true, message: '请填写商户号！',
                }],
              })(<Input />)}
          </FormItem>
          <FormItem label="结算日期">
            {getFieldDecorator('date', {
                rules: [{
                  required: true, message: '请选择结算日期！',
                }],
              })(
                <DatePicker style={{ width: '100%' }} />
              )}
          </FormItem>
          
          <Form.Item
            wrapperCol={{ span: 18, offset: 4 }}
          >
            <Button type="primary" htmlType="submit">下载</Button>
          </Form.Item>
        </Form>
      </Card>)
  }
}

export default Test;